

def generate_question(difficulty, theme, language):
    print(f"Generating question with the following parameters: {difficulty}, {theme}, {language}")
    return f"This is a test question with the following parameters: {difficulty}, {theme}, {language}"